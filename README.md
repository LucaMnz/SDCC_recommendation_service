# SDCC_recommendation_service

Il file contenente il modello addestrato deve essere messo in una directory chiamate "model", all'interno del progetto.

directory progetto:
      |
      |
      |___________>  src
      |
      |___________>  model
